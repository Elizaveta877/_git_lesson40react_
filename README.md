🚀 #Домашнє завдання: React-форми та Fetch API
Цей проєкт на React, створений за допомогою Vite, демонструє ключові концепції роботи з формами (контрольовані та неконтрольовані) та виконання асинхронних запитів до зовнішнього API для отримання даних.

✨ Основні можливості
Проєкт містить три основні секції:

Контрольована форма (ControlledForm):

Демонструє, як керувати станом полів введення (<input>, <select>, <checkbox>) за допомогою стану React (useState).

Дані форми оновлюються в режимі реального часу при кожній зміні (onChange).

Неконтрольована форма (UncontrolledForm):

Демонструє, як отримати значення полів введення безпосередньо з DOM за допомогою хука useRef лише під час надсилання форми (onSubmit).

Список користувачів (UsersList):

Використовує хук useEffect для виконання асинхронного запиту за допомогою fetch до тестового API (https://jsonplaceholder.typicode.com/users).

Обробляє стани завантаження (isLoading) та помилки (error).

Відображає список імен та електронних адрес отриманих користувачів.

🛠️ Технології
React (з хуками useState, useRef, useEffect)

Vite (як швидкий інструмент збірки)

JavaScript (ES6+)

Fetch API для роботи з даними.

💻 #Як запустити проект
Щоб запустити цей проєкт локально, виконайте наступні кроки:

1. Клонування репозиторію
Якщо ви ще не клонували проєкт, зробіть це:

Bash

git clone <URL_ВАШОГО_РЕПОЗИТОРІЮ>
cd lesson40 # Або назва вашої директорії
2. Встановлення залежностей
У кореневій директорії проєкту встановіть необхідні пакети:

Bash

npm install
3. Запуск сервера розробки
Запустіть додаток у режимі розробки за допомогою Vite:

Bash

npm run dev
4. Перегляд у браузері
Додаток буде доступний за адресою:

